#!/bin/sh

#AUTHOR : Tim Pattison
#SCRIPT : Activity12 - Compare Files


words1=()

IFS=$'\n' read -d ' ' -r  words1 < $1

echo "${words1[@]}"

#if cmp -s "$1" "$2"
#then
#	echo "The files are the same"
#else
#	echo "The files are different"
#	echo `comm $1 $2`
#fi
